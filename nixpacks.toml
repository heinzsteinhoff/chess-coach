[phases.setup]
aptPkgs = ["stockfish"]

[phases.install]
cmds = [
    "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install --upgrade pip setuptools wheel && pip install ."
]
paths = ["/opt/venv/bin"]
onlyIncludeFiles = ["pyproject.toml", "src"]
